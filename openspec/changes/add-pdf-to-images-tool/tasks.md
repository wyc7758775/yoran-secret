## 1. 方案确认
- [x] 1.1 确认 MuPDF WebAssembly 依赖、包体积、浏览器兼容性和 AGPL 许可是否适合当前公开仓库
- [x] 1.2 确认当前优先采用 MuPDF，无需降级到 PDF.js
- [x] 1.3 确认 zip 打包库，优先选择免费、前端可用、维护稳定的方案

## 2. 页面与入口
- [x] 2.1 在刚新增的 `/tools` 路由页面内新增 `PDF 转图片` 工具入口
- [x] 2.2 新增 `/tools/pdf-to-images` VitePress 路由
- [x] 2.3 新增 `PdfToImages.vue` 页面组件

## 3. 核心转换能力
- [x] 3.1 支持拖拽或选择多个 PDF 文件
- [x] 3.2 读取每个 PDF 的文件名、页数、大小和状态
- [x] 3.3 按队列逐页渲染 PDF 页面到图片
- [x] 3.4 支持 PNG / JPEG / WebP 输出格式
- [x] 3.5 支持 `省空间` / `标准` / `高清` / `极清` / `自定义` 输出清晰度
- [x] 3.6 支持设置 JPEG / WebP 图片质量
- [x] 3.7 页码默认转换全部页面，支持通过纯数字起止页转换指定范围

## 4. 批量导出
- [x] 4.1 支持单页图片下载
- [x] 4.2 支持单个 PDF 的全部页面 zip 下载
- [x] 4.3 支持全部 PDF 的转换结果 zip 下载
- [x] 4.4 zip 内按 PDF 文件名创建目录，图片按页码稳定命名
- [x] 4.5 全部打包支持切换为所有图片同一文件夹，并处理重名

## 5. 体验与可靠性
- [x] 5.1 显示总进度、单文件进度、当前页进度和失败状态
- [x] 5.2 支持取消当前转换任务
- [x] 5.3 支持清空已导入文件和已生成结果
- [x] 5.4 对加密 PDF、损坏 PDF、超大文件、内存不足给出明确错误提示
- [x] 5.5 转换完成后释放临时资源，避免长时间占用内存
- [x] 5.6 MuPDF 渲染引擎按需加载，并提供 loading 动效
- [x] 5.7 刷新页面后恢复尚未完成转换的待转换队列

## 6. 验证
- [x] 6.1 使用文字型 PDF 验证字体和清晰度
- [ ] 6.2 使用扫描型 PDF 验证图片页面输出
- [ ] 6.3 使用带透明、图片、矢量元素的 PDF 验证复杂页面
- [ ] 6.4 使用多文件批量导入验证 zip 结构
- [x] 6.5 执行 `pnpm run blog:build` 验证静态构建
